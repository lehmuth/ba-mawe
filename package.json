{
  "name": "ba-mawe",
  "version": "1.0.0",
  "description": "The code for the bachelor thesis of Manuel Kohler",
  "main": "src/index.html",
  "repository": "https://github.com/lehmuth/ba-mawe.git",
  "author": "Manuel Kohler",
  "private": true,
  "scripts": {
    "build": "echo 'No build necessary' && exit 0",
    "dev": "browser-sync start --server src --files src --port 8000 --no-ui --no-notify"
  },
  "devDependencies": {
    "browser-sync": "^2.26.14",
    "eslint": "^7.27.0",
    "prettier": "^2.3.0"
  }
}
